apiVersion: v1
kind: PersistentVolume
metadata:
  name: fitness-mongo-pv
spec:
  capacity:
    storage: 10Gi
    accessModes:
      - ReadWriteMany
    persistentVolumeReclaimPolicy: Retain
    nfs:
      path: /var/nfs/general
      server: 172.31.12.241